// const card = 2
/*

function onClick(div, numb){
    let item = div.getAttribute('id')

    const element = document.querySelectorAll(`${item} div[class*="item"]`)
    
    
    if(numb > 1){
     element.classList.add('.active')

    }
    
}




const getItems = document.querySelector('.items')

function showMeOnlyOne(eachItem){
    let array = new Array(getItems)
    let divInPage = ''

    const showDiv =

    divInPage.innerHTML =  `${array[0].getElementsByTagName('div')[eachItem].classList.add('.active')}`
    
}

showMeOnlyOne(2)



// .lastElementChild => perga o ultimo elemento do array















const getControls = document.querySelector('.controls')

function pages(totalPage, page) {

    let first = '';
    
    if (page > 1) {
        first += `<div onclick="onClick(items, 3)" class="first">&#171</div>`;
    }
     
    getControls.innerHTML = first
}

pages(4, 4)
 */
